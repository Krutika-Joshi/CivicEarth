services:
  - type: web
    name: civicearth-ml
    env: python
    region: virginia
    rootDir: ml
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13